% ============================================================================
% ESQUEMA (banca): a arquitetura hexagonal da biblioteca activelearning —
% as dependências apontam para dentro (adapters implementam as portas do
% domínio; casos de uso orquestram; a interface web fica na borda).
% Inserido no APÊNDICE D (a4-biblioteca/texto.tex) por ordem do autor
% (2026-08-23), dentro de figure:
%   \input{3-metodo/esquemas-propostos/esq-biblioteca-arquitetura}
% AUTOCONTIDO: sem \ref, sem números, sem códigos de experimento.
% Uso standalone (pré-visualização):
%   \documentclass[tikz,border=6pt,12pt]{standalone}
%   \usetikzlibrary{arrows.meta, positioning}
%   \begin{document}\input{esq-biblioteca-arquitetura.tex}\end{document}
% GEOMETRIA CALIBRADA PARA CORPO 12 (ppginf/book 12pt, textwidth 16cm).
% Requer apenas arrows.meta e positioning (já em packages.tex). Legível em
% P&B: núcleo = cinza escuro; camadas = branco; borda web = cinza claro.
% ============================================================================
\begin{tikzpicture}[
    x=1cm, y=1cm,
    caixa/.style={draw, rounded corners=2pt, align=center, text width=44mm,
                  minimum height=12mm, inner sep=3pt, font=\footnotesize},
    nucleo/.style={caixa, fill=gray!25, text width=88mm},
    borda/.style={caixa, fill=gray!8, text width=88mm},
    adap/.style={caixa, text width=36mm},
    seta/.style={-{Stealth[length=2.6mm]}, thick},
    rot/.style={font=\scriptsize, align=center, inner sep=2pt}
]
    \node[borda] (fb) at (5.5,0)
        {\textbf{FlowBuilder} (interface web): saneamento de dados,
         execução parametrizada de fluxos, curvas de aprendizado};
    \node[caixa, text width=88mm] (app) at (5.5,-2.5)
        {\textbf{casos de uso}: avaliação de oráculo $\cdot$ laço de
         aprendizado ativo $\cdot$ \textit{runner} do FALCO $\cdot$
         saneamento};
    \node[nucleo] (dom) at (5.5,-5.1)
        {\textbf{domínio} (núcleo puro, sem rede nem \textit{framework}):
         instâncias $\cdot$ \textit{CategorySchema} $\cdot$ anotações com
         contabilidade de uso $\cdot$ estratégias de incerteza $\cdot$
         métricas};

    % adapters na base: implementam as portas; dependência aponta para dentro
    \node[adap] (ad1) at (0,-8.4)
        {\textbf{oráculos}:\\provedores de LLM\\e simulado com ruído};
    \node[adap] (ad2) at (3.7,-8.4)
        {\textbf{classificadores}:\\PVBin; BERTimbau};
    \node[adap] (ad3) at (7.4,-8.4)
        {\textbf{estratégias e dados}:\\DRI-SL; CSV do domínio};
    \node[adap] (ad4) at (11.1,-8.4)
        {\textbf{persistência}:\\SQLite; PostgreSQL};

    \draw[seta] (fb) -- (app);
    \draw[seta] (app) -- (dom);
    \draw[seta] (ad1.north) -- (dom.230);
    \draw[seta] (ad2.north) -- (dom.250);
    \draw[seta] (ad3.north) -- (dom.290);
    \draw[seta] (ad4.north) -- (dom.310);

    % a nota fica ABAIXO da fileira de adapters: entre as camadas ela era
    % atravessada pelas setas diagonais (achado da inspeção visual)
    \node[rot] at (5.5,-9.7)
        {\textit{adapters} implementam as portas definidas pelo domínio:
         toda dependência aponta para dentro};
\end{tikzpicture}
