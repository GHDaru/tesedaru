% ============================================================================
% ESQUEMA (banca, v2 — polida na tarefa 0138): pré-processamento e os DOIS
% espaços de rótulos da tese (Seção sec:metodo-dados-preproc). A figura é
% ESTRUTURAL: mostra a bifurcação (a mesma base normalizada contada de dois
% jeitos, com papéis distintos) e deixa TODAS as contagens para a prosa ao
% lado — v1 carregava os números da seção; a restrição da série (zero
% números medidos em figura, que escapa dos scripts de checagem) os
% removeu nesta versão, por ordem do principal/autor.
% Uso previsto (o principal gateia; a banca NÃO edita o Cap. 3):
%   fim da Seção sec:metodo-dados-preproc, dentro de figure:
%   \input{3-metodo/esquemas-propostos/esq-preproc-espacos-rotulos}
% Uso standalone (pré-visualização):
%   \documentclass[tikz,border=6pt,12pt]{standalone}
%   \usetikzlibrary{arrows.meta, positioning}
%   \begin{document}\input{esq-preproc-espacos-rotulos.tex}\end{document}
%   (no modo standalone, troque as \ref{...} por texto fixo ou defina-as)
% GEOMETRIA CALIBRADA PARA CORPO 12 (ppginf/book 12pt, textwidth 16cm) —
% a v1 era de corpo 10. Requer apenas arrows.meta e positioning. Legível em
% P&B: entrada = cinza claro; operação = branco; produto = cinza médio.
% SEM números, SEM códigos internos, SEM caminhos, SEM travessões.
% ============================================================================
\begin{tikzpicture}[
    x=1cm, y=1cm,
    caixa/.style={draw, rounded corners=2pt, align=center, text width=50mm,
                  minimum height=13mm, inner sep=3pt, font=\footnotesize},
    entrada/.style={caixa, fill=gray!8},
    produto/.style={caixa, fill=gray!14},
    seta/.style={-{Stealth[length=2.6mm]}, thick},
    rot/.style={font=\scriptsize, align=center, inner sep=2pt}
]
    \node[entrada, text width=46mm] (base) at (5.6,0)
        {base corrigida pela auditoria\\(Seção~\ref{sec:metodo-dados-auditoria}):
         linhas de\\descrição curta $+$ rótulo};
    \node[caixa, text width=52mm] (norm) at (5.6,-2.7)
        {\textbf{normalização mínima}:\\minúsculas e remoção de acentos;\\
         variações lexicais ficam com o\\tokenizador de subpalavras};

    % ------- bifurcação: a mesma base, contada de dois jeitos --------------
    % (títulos de coluna na 1ª linha das caixas: soltos, eram atravessados
    % pelas setas — achado do loop da v1)
    \node[caixa] (filtro) at (1.6,-5.8)
        {\textbf{espaço dos oráculos}\\(contado sobre as linhas):\\
         classes muito raras agrupadas\\na sentinela \texttt{\_rare\_}};
    \node[caixa] (dedup) at (9.6,-5.8)
        {\textbf{visão deduplicada}\\(textos únicos): mantém a\\
         primeira ocorrência de cada\\texto; filtro brando de classes};

    \node[produto] (catsch) at (1.6,-8.9)
        {\textit{CategorySchema}: espaço\\\textbf{fechado} de categorias,\\
         fonte única da restrição de\\saída dos oráculos
         (Seção~\ref{sec:metodo-oraculo-instrumentacao})};
    \node[produto] (pop) at (9.6,-8.9)
        {todas as classes presentes,\\sem \texttt{\_rare\_} (cauda longa\\
         preservada): experimentos em\\escala populacional
         (Capítulo~\ref{ch:resultados-falco})};

    % ------- setas (depois dos nós) ----------------------------------------
    \draw[seta] (base) -- (norm);
    \draw[seta] (norm.south west) -- (filtro.north);
    \draw[seta] (norm.south east) -- (dedup.north);
    \draw[seta] (filtro) -- (catsch);
    \draw[seta] (dedup) -- (pop);

    \node[rot, anchor=west, text width=136mm, align=left] at (-1.0,-10.9)
        {duas chaves de texto convivem: o censo da auditoria compara
         descrições com os espaços internos colapsados; a deduplicação usa a
         descrição apenas aparada e em minúsculas. As contagens de cada
         espaço estão no texto desta seção, e uma receita executável
         versionada as reproduz.};
\end{tikzpicture}
